This folder is reserved for build resources used by electron-builder.
Place icons here with the following names:

- `icon.ico` for Windows
- `icon.icns` for macOS
- `icon.png` for Linux/AppImage

You can create simple placeholder icons, or replace them with proper icons before building.
